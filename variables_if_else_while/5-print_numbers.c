#include <stdio.h>
#include <stdlib.h>
/**
 * main - Prints the alphabet uppercase and lowercase letters
 * Return: Always (Success)
 */

int main(void)
{
	int n = 0;

	for (n = 0; n < 10; n++)
	printf("%d", n);
	printf("\n");

	return (0);
}
