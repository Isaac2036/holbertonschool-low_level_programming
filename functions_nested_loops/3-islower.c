#include "main.h"

/**
 * _islower - check if c is lowercase o uppercase
 * @c: The character to be checked
 * Return: 1 if c is lowercase and 0 if c is otherwise
 */

int _slower(int c)
{
if ( c >= 97 && c <= 122)
{
	return (1);
}
	return (0);
}
